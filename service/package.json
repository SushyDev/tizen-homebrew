{
  "name": "tizen-homebrew-service",
  "version": "0.1.6",
  "description": "On-TV install service: serves the phone UI and drives the TV's own SDB daemon.",
  "main": "src/main.js",
  "license": "GPL-3.0-only",
  "scripts": {
    "build": "node build.js",
    "start": "node src/main.js",
    "test": "node test/run.js && node test/relay.js && node test/lockout.js && node test/pipeline.js && node test/preview.js && node test/resign.js && node test/signature.js && node test/verdicts.js && node test/updates.js && node test/log.js && node test/config.js && node test/runtime.js && node test/packages.js && node test/platform.js && node test/fetch.js && node test/repl.js && node test/memory.js",
    "check": "node check-syntax.js dist/index.js"
  },
  "dependencies": {
    "ws": "^8.21.3"
  },
  "devDependencies": {
    "acorn": "^8.14.0",
    "rolldown": "^1.2.6"
  }
}
