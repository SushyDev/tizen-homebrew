node_modules/
dist/
release/
.ncc/
.package/
.last-build-size
*.wgt
*.p12
*.log

# npm workspaces: only the root lockfile is authoritative.
*/package-lock.json
!/package-lock.json

# macOS finder metadata
.DS_Store

# A read-only checkout of Samsung/lwnode, not part of the build.
lwnode/
